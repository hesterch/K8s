apiVersion: v1
kind: Service
metadata:
  labels:
    app: result
  name: resultservicelbc
spec:
  type: LoadBalancer 
  ports:
  - protocol: TCP 
    port: 80 
    targetPort: 80
  selector:
    app: result
